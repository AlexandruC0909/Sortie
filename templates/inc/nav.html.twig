<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <img src="{{ asset('/resources/images/sortir.svg') }}" alt="">
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">

            {% if app.user %}
                <li>
                    <a class="navbar-brand" href="{{ path('sortie_list') }}">Home</a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('participant_participant', {id: app.user.id}) }}">Salut {{ app.user.nom }} </a>
                </li>

            {% endif %}
            {% if app.user %}
            {% if app.user.administrator == 1 and is_granted('ROLE_ADMIN')   %}
                <li class="nav-item active">
                    <a  class="nav-link" href="{{ path('administrator_administrator') }}">Espace Admin</a>
                </li>

            {% endif %}
            {% endif %}
            {% if app.user %}
                <li class="nav-item active">
                    <a  class="nav-link" href="{{ path('sortie_newSortie') }}">Créer une sortie</a>
                </li>
                <li class="nav-item active">
                    <a  class="nav-link" href="{{ path('app_logout') }}">Se déconnecter</a>
                </li>
            {% else %}
               {#<li class="nav-item active">
                    <a class="nav-link" href="{{ path('app_register') }}">Register</a>
                </li> #}
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('app_login') }}">Se connecter</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>